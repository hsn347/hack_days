# docker-compose.yml
# تشغيل 5 بوتات لكل حساب تلقائياً

version: '3.8'

services:
  bot_1:
    build: .
    restart: unless-stopped
    volumes:
      - ./session_cache.json:/app/session_cache.json:ro
      - ./logs/bot1.log:/app/onemt_bot.log
    command: ["--email", "azjfhf48@gmail.com", "--password", "any", "--interval", "300"]
    
  bot_2:
    build: .
    restart: unless-stopped
    volumes:
      - ./session_cache.json:/app/session_cache.json:ro
      - ./logs/bot2.log:/app/onemt_bot.log
    command: ["--email", "hmzawyha44@gmail.com", "--password", "any", "--interval", "300"]
    
  bot_3:
    build: .
    restart: unless-stopped
    volumes:
      - ./session_cache.json:/app/session_cache.json:ro
      - ./logs/bot3.log:/app/onemt_bot.log
    command: ["--email", "burcudemr@gmail.com", "--password", "any", "--interval", "300"]
    
  bot_4:
    build: .
    restart: unless-stopped
    volumes:
      - ./session_cache.json:/app/session_cache.json:ro
      - ./logs/bot4.log:/app/onemt_bot.log
    command: ["--email", "king7moe1990@gmail.com", "--password", "any", "--interval", "300"]
    
  bot_5:
    build: .
    restart: unless-stopped
    volumes:
      - ./session_cache.json:/app/session_cache.json:ro
      - ./logs/bot5.log:/app/onemt_bot.log
    command: ["--email", "johan2003@yopmail.com", "--password", "any", "--interval", "300"]
